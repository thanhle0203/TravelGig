$(document).ready(function() {
	$("#fetch").click(function() {
		$.get("http://localhost:8282/trainer/" + $("#stdName").val(), {
		}, function(responseText) {
			$("#trainerId").val(responseText.id);
			$("#trainerName").val(responseText.name);
			$("#trainerAge").val(responseText.age);
		});

	});
});